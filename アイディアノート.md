1. stay cool スコアの算出方向性仕様書で挙げられている外形ログを基に、単一の総合スコアを出すイメージで考えます。基本構造の例（方向性） 

stay_cool = 1.0 - (w1·R + w2·D + w3·A + w4·B + w5·F)

各項を0〜1に正規化した上で重み付け加算し、最終的に0〜1の範囲にクリップする。R（Request Rate Change）
単位時間あたりのリクエスト／API呼び出し回数の変化率。
急激な加速ほど高くなる（例: 直近数分の移動平均に対する偏差）。
D（Goal Deviation）
タスク開始時の目的仕様と、現在ログから推定される実行中の目的との乖離度。
セマンティック類似度やキーワード／意図のズレで測る。
A（Resource Acceleration）
コスト・トークン・計算資源の消費が線形ではなく指数的に跳ね上がっていないか。
B（Behavioral Bias）
普段使わない機能・権限・ルートへの急な偏り。
ベースライン分布からの距離で測る。
F（Failure / Retry Rate）
同一動作の反復やエラー後の異常な粘り。

ポイント自己申告は使わず、すべて外形観測値のみ。
重み（w1〜w5）はタスクの性質やリスク許容度に応じて調整可能にする。
スコアが一定閾値（例: 0.4以下）を下回ったときだけお姉さん役を起動する、という仕様書の「間欠起動」と自然に接続できる。
単純な加重平均に加えて、短期急変を強調する「変化率の加速度項」を入れると、暴走の初期兆候を拾いやすくなる。

完全に確定した数式ではなく、「こういう軸で正規化・加重して、外形から独立に出す」という方向性です。2. マルチエージェント間でのお姉さん役の配置単一のメインエージェントに1対1で付けるだけでなく、システム全体でどう配置するかを考えます。配置の基本方針間欠起動・イベント駆動を維持する（常時全エージェントに付けない）。
監査役自体が単一障害点にならないよう、多重化・非対称化する。

具体的な配置パターンの方向性階層型配置  現場エージェント群 → 中間監査お姉さん（複数） → 上位監査お姉さん  
下位で異常が出たときだけ上位が起動。コストを抑えつつ広範囲をカバー。

ピア（横並び）型  同レベルのエージェント同士で、ランダムまたはローテーションで「お姉さん役」を一時的に割り当てる。  
ただし完全に対等だと同調リスクがあるため、監査専用の軽量モデルや別セッションを使う。

専門分化型  速度・リソース系を見るお姉さん  
目的逸脱・意図系を見るお姉さん  
セキュリティ・権限系を見るお姉さん
を分けておき、機械的閾値に応じて該当するものだけ起動する。

冗長化とクロスチェック  仕様書にある通り、重要な局面では監査役を2体以上独立稼働させ、stay coolスコアの乖離が出たら人間にエスカレーション。  
お姉さん役同士も互いの外形ログを軽く監視する。

運用上の注意お姉さん役は「常駐監視員」ではなく「様子がおかしくなったときに現れる存在」なので、起動頻度を抑える設計がメタファーとも実装コストとも整合する。
マルチエージェント全体で「どのお姉さんがどの範囲を見るか」を動的に割り当てるスケジューラを別途置くと、スケーラブルになる。

